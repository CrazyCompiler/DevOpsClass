---
- hosts: box1
  vars_files: 
    - ./config/config.yml 
  tasks:
    - name: Include nodejs role
      include_role: 
        name: nodejs
      vars:
        node_version: '7.6.0'
    - name: Include nginx role
      include_role: 
        name: nginx

- hosts: box2
  vars_files: 
    - ./config/config.yml 
  tasks:
    - name: Include nodejs role
      include_role: 
        name: nodejs  
      vars:
        node_version: '7.0.0'  

 
