---
  - name: Copy nginx config
    become: yes
    copy: 
      src: ./config/default.conf
      dest: /etc/nginx/conf.d/default.conf

  - name: restart nginx
    become: yes
    service:
      name: nginx
      state: restarted
      enabled: yes